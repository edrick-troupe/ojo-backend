/*
  async updateBookEvent(_, { id, input }, { dataSources }) {
    const event = await dataSources.ojoDB.bookEventDatamapper.updateBooking(id, input);
    return event;
  },

  async deleteBookEvent(_, { id }, { dataSources }) {
    const result = await dataSources.ojoDB.bookEventDatamapper.deleteBooking(id);
    return result;
  },
*/
